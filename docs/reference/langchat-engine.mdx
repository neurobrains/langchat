---
title: LangChatEngine
description: Core engine API reference
---

import { Param } from 'mintlify';

## Class: LangChatEngine

Core orchestrator for LangChat components.

### Constructor

```python
LangChatEngine(
    llm,
    vector_db,
    db,
    reranker=None,
    prompt_template=None,
    standalone_question_prompt=None,
    verbose=False,
    max_chat_history=20
)
```

### Methods

#### `async chat()`

Process a chat query.

```python
async def chat(
    self,
    query: str,
    user_id: str,
    domain: str = "default",
    standalone_question: Optional[str] = None
) -> dict
```

#### `get_session()`

Get or create a user session.

```python
def get_session(
    self,
    user_id: str,
    domain: str = "default"
) -> UserSession
```

### Properties

- `llm` - LLM provider instance
- `vector_adapter` - Vector database adapter
- `reranker_adapter` - Reranker adapter
- `history_store` - Database adapter
- `sessions` - Active user sessions

## Next Steps

- **[LangChat API](/api-reference/langchat)** - Main API
- **[Adapters](/adapters/overview)** - Adapter docs

---

**Built with ❤️ by [NeuroBrain](https://neurobrain.co)**
